<!-- set_new_gift.xml -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@drawable/setchild_background">


    <!-- Homepage ImageButton -->
    <ImageButton
        android:id="@+id/gift_BTN_back"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@drawable/home_icon"
        android:layout_marginTop="10dp"
        android:layout_marginStart="5dp"
        android:background="@android:color/transparent"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true" />

    <!-- Title -->
    <TextView
        android:id="@+id/gift_TXT_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="New Gift"
        android:textSize="30sp"
        android:layout_below="@+id/gift_BTN_back"
        android:fontFamily="@font/dosis"
        android:textStyle="bold"
        android:layout_marginTop="40dp"/>

    <EditText
        android:id="@+id/gift_TXT_giftName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/dosis"
        android:layout_below="@id/gift_TXT_title"
        android:hint="Gift Name"
        android:inputType="text"/>

    <EditText
        android:id="@+id/gift_TXT_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/dosis"
        android:layout_below="@id/gift_TXT_giftName"
        android:hint="Gift Description"
        android:inputType="text"/>

    <EditText
        android:id="@+id/gift_TXT_coinsVal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/dosis"
        android:layout_below="@id/gift_TXT_description"
        android:hint="Coins Value"
        android:inputType="number"/>

    <Button
        android:id="@+id/gift_BTN_createGift"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/dosis"
        android:layout_below="@id/gift_TXT_coinsVal"
        android:text="Create Gift"/>

</RelativeLayout>
